# DEV-1618, DEV-1622, and DEV-1304 implementation plan

## Implementation waves

1. Add bounded platform detection, provenance-aware product routes, optional local rendering, and CLI conflict checks.
2. Add adaptive catalog discovery, specialized extractors, sitemap/hydration limits, cached evidence, and exact products-phase candidate validation.
3. Tighten image adapters, add stable gate/audit telemetry, and provide a production-parity no-business-write evaluator.

## Verification

- Run focused service and parser tests, then `pnpm typecheck`, `pnpm lint`, and `pnpm build`.
- Inspect test drift and the complete diff.
- Run the live adapter cohorts and staged rollout only with a provisioned non-production environment and explicit production approval.

## Pre-mortem

- The assumption most likely to invalidate the approach is that channel pages expose enough static or locally rendered evidence to reach 20 complete triples.
- The silent risks are shared-marketplace contamination and pre-storage image loss; exact evidence membership and per-method rejection summaries are the safeguards.
