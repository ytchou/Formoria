# E2E Author Plan — 2026-07-23
**Input:** diff (feat/dev-1145-onboarding-welcome-card vs origin/main)
**Branch:** feat/dev-1145-onboarding-welcome-card

## Journeys to author/refresh (this run)

### 1. Owner dismisses dashboard welcome card — persists across reload  [NEW spec]
- **Actor:** authenticated-owner (isolated or ensureOwnedBrand pattern)
- **Goal:** New owner sees the welcome card with 4 tip links on the brand overview; dismissing it hides it permanently.
- **Pre-conditions:** Owned `[E2E-TEST]` brand with `onboarding_dismissed_at` null (fresh seed satisfies this).
- **Steps:** 1. Navigate to `/dashboard/brands/<slug>`. 2. Assert welcome card visible with 4 tip links (edit profile → `/dashboard/brands/<slug>/edit`, health → `#profile-completeness`, analytics → `/dashboard/brands/<slug>/analytics`, FAQ → `/faq#for-owners`). 3. Click dismiss. 4. Assert card gone. 5. Reload page. 6. Assert card still gone (server-persisted).
- **Expected (observable):** Card with 歡迎 title + 4 links; after dismiss + reload, card absent while `[data-testid="brand-profile"]` remains visible.
- **Risk if broken:** New owners lose their only guided entry to dashboard features; dismiss failing to persist re-nags every visit.
- **DB / auth state:** `[E2E-TEST]` self-seed owned brand; dismiss mutates `brands.onboarding_dismissed_at` (cleaned by teardown sweep).
- **Target project:** deep
- **Destination spec:** `e2e/tests/dashboard-welcome-card.spec.ts`

### 2. FAQ page sections + anchors  [NEW spec — deepens shallow smoke coverage]
- **Actor:** anonymous
- **Goal:** Visitor finds General and For Brand Owners FAQ sections; owner-FAQ deep links land correctly.
- **Pre-conditions:** none
- **Steps:** 1. Goto `/faq`, assert two level-2 section headings (一般問題 / 品牌主專區) and 19 `<details>` items. 2. Goto `/faq#for-owners`, assert `#for-owners` section is in viewport. 3. Goto `/faq#claim`, assert the `#claim` `<details>` is auto-opened.
- **Expected (observable):** Both headings visible; 19 details; `#claim` has `open` attribute after hash navigation.
- **Risk if broken:** Welcome-card FAQ tip link and legacy `#claim` deep links land on a broken/unsplit page.
- **DB / auth state:** none
- **Target project:** deep
- **Destination spec:** `e2e/tests/faq.spec.ts`

## Carried to catalog (not this run)
- Welcome-card i18n EN variant (`/en` locale copy) — P3; zh-TW is primary, i18n-en.spec.ts covers locale switching generally.
- Smoke-tier FAQ section check (3-browser) — P3; existing visitor smoke still passes (asserts h1 + first details only); promote only if owner FAQ becomes a conversion path.
