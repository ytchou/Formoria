# DEV-1372 E2E Authoring Report

## Authored and refreshed

- Added one desktop `@smoke` Creative Expo journey covering map → list synchronization, category composition, contextual count agreement, official/Romanized/booth search, global reset, permanent brand navigation/Back, and direct `?zone=K2&category=crafts` hydration.
- Added deep coverage for localized zh-TW/en explorer/source copy, official map and roster attribution, server-rendered brand links, and a failed map image with a still-usable result list.
- Updated the pre-existing generic event area-filter selector to remain exact after the Creative Expo nested explorer introduced another named region.
- Added a Pixel 5 journey for `[地圖] [品牌名單]` switching, filter/search state retention between panels, durable zone/category URL context through brand navigation/Back, and successful result navigation.
- Existing Event JSON-LD and reciprocal canonical/hreflang cases remain in the same spec and passed on the corrected deep run. The server-HTML assertion preserves the static brand-link contract without duplicating those SEO assertions.

## Run results

- Deep initial authoring run: `2 passed, 4 failed` in 2.0 minutes. Three failures were selector/request-authoring drift and one was the newly added smoke journey stopping on an optional transient Back-query expectation.
- Deep single allowed correction rerun: `4 passed, 1 skipped, 1 failed` in 1.6 minutes. Event JSON-LD, canonical/hreflang, localized/source/server-HTML, and failed-map/list usability passed. The generic area-filter case skipped because this event does not expose its legacy area chips. The smoke case stopped because transient query text resets on Back while the durable event URL returns correctly.
- The optional Back-query expectation was corrected to assert the durable event context and a usable search after returning. Per the one-rerun limit, the deep project was not run a third time.
- Mobile run: `7 passed` in 1.4 minutes, including the new Creative Expo journey.
- Focused ESLint: passed for both changed specs.
- `tsc --noEmit`: passed. The repository excludes E2E from project type-checking; Playwright compilation exercised the authored specs.
- Live Pixel 5 discovery measured document/client widths at `393 / 393`; no geometry assertion was added because the authoring brief explicitly prohibited geometry assertions.
- Post-fix reviewer scope: `4/4 passed` against one worktree-owned `localhost:3017` server — three Creative Expo desktop cases and the Creative Expo Pixel 5 case. The direct `?zone=K2&category=crafts` journey now hydrates the Crafts & Art filter, and the exact official roster attribution URL is asserted.

## Historical defect and resolution

- The initial authoring run was RED: loading `/en/events/2026-taiwan-creative-expo?zone=K2&category=crafts` selected K2 but left `All categories` pressed and reported all 33 K2 brands. The URL value was rejected because the roster stored the localized category value `工藝文創`; interactive selection also leaked that internal value into the query string.
- Resolved in the follow-up fix: recognized stored category values hydrate from canonical slugs and serialize back to canonical slugs. The post-fix direct URL selects both K2 and Crafts & Art, and the 4/4 scoped reviewer run passed.

## Carried backlog

- Canonical empty roster (`[]`) and roster-read failure (`null`) cannot be safely induced against the shared canonical event without mutating ambient data or mocking an internal service. Add a dedicated fixture/preview mode before authoring these cases.
- A dedicated automated horizontal-overflow assertion is deferred under the no-geometry constraint; live Pixel 5 discovery found no overflow.
