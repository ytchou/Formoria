# DEV-1503 E2E de-drift report

Run ID: `2026-08-19-0357-dev-1503`

## Authored

- De-drifted 20 existing deep specs and two shared seed helpers from the removed product-type/product-tag contracts to `category`, `subcategories`, and `subcategories_en`; the repository-wide zero scan added `i18n-en.spec.ts` to the original 21-path inventory.
- Updated the directory taxonomy import to `L1_CATEGORIES`.
- Updated correction field identifiers to `category` and `subcategories`, and the owned-brand editor selector to `#categorySlug`.
- Preserved public URLs and query parameters (`?category=`, `?sub=`, `/categories/*`).
- Added no new `.spec.ts` files or user journeys.

## Live observation

- Served this linked worktree at `http://127.0.0.1:3213` and observed it with Playwright browser tooling.
- Confirmed the category landing remains `/en/categories/crafts`, the public category filter is exposed as `Category`, and the zh-TW correction trigger remains `資料有誤?`.
- Surfaced an application copy defect outside this authoring scope; its source was corrected in commit `0cf6e57d` to render `品牌類別` / `Brand category` and `商品子類別` / `Product subcategory`.
- Added final-copy assertions to the existing brand-detail smoke case so the corrected copy remains the pass condition.

## Runtime evidence

- A local attempt failed closed in `globalSetup` at the staging-target preflight before tests or database mutations, as designed.
- The first canonical HTTPS staging run exercised deployed merge SHA `261b1667` under `deep`: all 104 tests completed and eight failed. The command was interrupted only after completion when the HTML reporter auto-opened its report server.
- Four correction mutation cases reached the expected UI but canonical staging rejected their Next server actions with `403` and `{"error":"This flow is disabled in staging"}`. Those cases now skip only on the canonical staging origin; the read-only correction-control case still runs there.
- The search contract returned the stable final TypeScript key `categoryLabel`; the spec no longer expects the removed `category` response key.
- Newsletter confirmation remains a `307` redirect outside staging, while newsletter and email unsubscribe routes retain their non-staging `200`/`400` contracts; all five route cases now assert canonical staging's exact `403` lockdown JSON.
- The online-channel form now asserts that physical-only `地點分類` is absent while independently requiring and selecting the still-visible `地區` control before URL submission.
- Share-card output remains publicly cacheable outside staging and now asserts canonical staging's `private, no-store` response finalization.
- The focused four-spec rerun collected 42 tests: 33 passed, four correction mutations skipped on canonical staging, three serial API cases did not run, and two failed. The first API failure showed that newsletter unsubscribe shares the same staging lock as confirm and masked the remaining serial cases; the form failure showed that only location type disappears for online channels, not region.

## Repair rerun

Status: passed against canonical HTTPS staging on deployed application merge SHA `261b1667`, using repaired test commit `1fc3190b`.

```bash
PLAYWRIGHT_HTML_OPEN=never BASE_URL=https://staging.formoria.com pnpm exec playwright test --project=deep \
  e2e/tests/api-contracts.spec.ts \
  e2e/tests/brand-detail.spec.ts
```

Static authoring checks completed: the expanded 22-TypeScript-file inventory contains zero legacy taxonomy identifiers and `git diff --check` is clean.

The final two-spec rerun passed all 33 tests in 3.1 minutes. Global teardown then passed the strict run-scoped E2E namespace audit; the temporary environment link and generated auth state were removed, and no report server remained.

## Carried items

- None.
