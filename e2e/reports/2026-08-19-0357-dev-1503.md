# DEV-1503 E2E de-drift report

Run ID: `2026-08-19-0357-dev-1503`

## Authored

- De-drifted 20 existing deep specs and two shared seed helpers from the removed product-type/product-tag contracts to `category`, `subcategories`, and `subcategories_en`; the repository-wide zero scan added `i18n-en.spec.ts` to the original 21-path inventory.
- Updated the directory taxonomy import to `L1_CATEGORIES`.
- Updated correction field identifiers to `category` and `subcategories`, and the owned-brand editor selector to `#categorySlug`.
- Preserved public URLs and query parameters (`?category=`, `?sub=`, `/categories/*`).
- Added no new `.spec.ts` files or user journeys.

## Live observation

- Served this linked worktree at `http://127.0.0.1:3213` and observed it with Playwright browser tooling.
- Confirmed the category landing remains `/en/categories/crafts`, the public category filter is exposed as `Category`, and the zh-TW correction trigger remains `資料有誤?`.
- Surfaced an application copy defect outside this authoring scope; its source was corrected in commit `0cf6e57d` to render `品牌類別` / `Brand category` and `商品子類別` / `Product subcategory`.
- Added final-copy assertions to the existing brand-detail smoke case so the corrected copy remains the pass condition.

## Verification handoff

The orchestrator's local attempt failed closed in `globalSetup` at the staging-target preflight before any test or database mutation. This is expected: the canonical suite rejects a local `BASE_URL` even when the local app uses the staging Supabase project.

The only pending gate is one run of the 20 changed specs under `deep` (104 statically inventoried test cases) against canonical HTTPS staging after PR3 is merged and that exact SHA is deployed:

```bash
BASE_URL=https://staging.formoria.com pnpm exec playwright test --project=deep \
  e2e/tests/admin-brand-refresh.spec.ts \
  e2e/tests/admin-dashboard.spec.ts \
  e2e/tests/admin-moderation.spec.ts \
  e2e/tests/admin-submission-enrichment.spec.ts \
  e2e/tests/admin-submission-publishable-core.spec.ts \
  e2e/tests/api-contracts.spec.ts \
  e2e/tests/brand-city.spec.ts \
  e2e/tests/brand-corrections.spec.ts \
  e2e/tests/brand-detail.spec.ts \
  e2e/tests/brand-save.spec.ts \
  e2e/tests/claim-lifecycle.spec.ts \
  e2e/tests/claim-smoke.spec.ts \
  e2e/tests/dashboard-brand-owned-edit.spec.ts \
  e2e/tests/dashboard-welcome-card.spec.ts \
  e2e/tests/directory.spec.ts \
  e2e/tests/i18n-en.spec.ts \
  e2e/tests/mit-verification.spec.ts \
  e2e/tests/public-data-boundary.spec.ts \
  e2e/tests/search-edge-cases.spec.ts \
  e2e/tests/share-card-api.spec.ts
```

Static authoring checks completed: the expanded 22-TypeScript-file inventory contains zero legacy taxonomy identifiers and `git diff --check` is clean. Exact-SHA staging runtime pass/fail and run-scoped database cleanup remain for the orchestrator to certify.

## Carried items

- None. The application copy defect was resolved in `0cf6e57d`; only the exact-SHA staging runtime gate remains.
