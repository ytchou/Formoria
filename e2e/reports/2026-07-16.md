# E2E Author Report — 2026-07-16

## Scope

- Shared detailed owner-submission and dashboard brand-edit wizard sections.
- Dashboard draft persistence versus submission final-only persistence.
- Romanized-name URL preview and review-governed canonical URL changes.
- Inline social, purchase, and custom link rows.

## Authored and refreshed coverage

- `e2e/tests/submit-funnel.spec.ts`
  - Completes the four-step detailed owner wizard.
  - Proves no `brand_submissions` row exists after intermediate navigation.
  - Uploads the required hero image and submits once from the final step.
  - Verifies `romanized_name`, explicit `purchase_website`, and custom `other_urls` in the stored submission.
- `e2e/tests/dashboard-brand-owned-edit.spec.ts`
  - Updates the native price-range selector interaction.
  - Verifies the romanized-name public URL preview.
  - Verifies three link groups, six fixed inline platform rows, and dashboard-only official-site required state.

## Verification

- Playwright collection: passed; 16 tests collected across the two affected specs.
- Browser execution: blocked because the configured E2E Supabase schema does not yet contain `brands.romanized_name`; migration `20260716094606_add_brand_romanized_name_slug_redirects.sql` must be applied first.
- Full Vitest suite passed.
- Full lint and frontend token guard passed with 12 pre-existing warnings and no errors.
- TypeScript validation passed.

## Remaining risk

- The migration could not be exercised in this worktree because no local Supabase database was running and the checkout was not linked to a remote project.
