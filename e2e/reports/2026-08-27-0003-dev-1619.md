# DEV-1619 E2E authoring report

## Result

Replaced the former brand-level MIT journey with a self-seeded product journey in
`e2e/tests/mit-verification.spec.ts`.

The journey asserts that a qualified product shows `台灣製造` on its brand page,
discovery trail, and homepage tile; an unqualified sibling does not; and neither
the brand header nor directory card inherits the badge.

## Verification

Static TypeScript, lint, and timeout-budget checks are part of the branch
verification. Browser execution is pending deployment to isolated staging: the
local configured Supabase schema does not yet contain the product-origin
migrations, and the canonical suite intentionally rejects non-staging targets.

## Cleanup

The spec seeds uniquely named `[E2E-COMMUNITY]` brands and deletes them by captured
IDs in `afterAll`; foreign-key cascades remove the products, sources, and trail
selection.
