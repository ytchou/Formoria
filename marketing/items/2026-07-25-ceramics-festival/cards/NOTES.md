Card 03 ("Formoria 收錄的參展品牌") lists 3 placeholder slots. The brief's
`Reference brands` are unresolved placeholders (`placeholder-ceramics-1/2/3`)
and this session has no database access to look up which real, listed
ceramics brands are exhibiting at the biennial. Swap in the 3 actual brand
names (and consider a `brand-highlight.html` card per brand instead of one
bullet list) once that data is available, then re-render.
